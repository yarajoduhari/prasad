replicaCount: 1
labels: 
image:
  registory: localhost:8082
  repository: kube-jenkins
  pullPolicy: Always
  # Overrides the image tag whose default is the chart appVersion.
  tag: ""

#imagePullSecrets:
#  - name: nobroker-nexus-keys

imageCredentials:
  registry: localhost:8082
  username: admin
  password: admin123
#  email: devops@nobroker.in

commonAnnotations: {}

#extraEnvVars:
#  - name: PROFILE
#    value: "blackbox"
#  - name: INFRA
#    value: "star"
#  - name: LOCAL_SETUP
#    value: "false"

podAnnotations: {}

podSecurityContext: {}
  # fsGroup: 2000

#securityContext: {}
  # capabilities:
  #   drop:
  #   - ALL
  # readOnlyRootFilesystem: true
  # runAsNonRoot: true
  # runAsUser: 0

serviceAccount:
  # Specifies whether a service account should be created
  create: true
  # Annotations to add to the service account
  annotations: {}
  # The name of the service account to use.
  # If not set and create is true, a name is generated using the fullname template
  name: ""

service:
  type: NodePort
  port: 8080

containerPorts:
  http: 8080

#ingress:
#  enabled: true
#  annotations: 
#    kubernetes.io/ingress.class: traefik-ingress
#    traefik.ingress.kubernetes.io/router.entrypoints: websecure
#    traefik.ingress.kubernetes.io/router.tls: "true"
#    traefik.ingress.kubernetes.io/router.tls.certresolver: letsencrypt
#    traefik.ingress.kubernetes.io/router.middlewares: traefik-star-ipwhitelist@kubernetescrd
#    traefik.ingress.kubernetes.io/service.passhostheader: "true"
#  hosts:
#    - host: star-testing.nobroker.star.nobroker.in
#      paths:
#        - path: /
#          pathType: ImplementationSpecific
      
resources:
   limits:
     cpu: 200m
     memory: 600Mi
   requests:
     cpu: 100m
     memory: 500Mi
     
nodeSelector: 
  kubernetes.io/hostname: minikube
